var express = require('express');
var router = express.Router();

/* GET home page. */
router.get('/', function (req, res, next) {
  res.render('index', { title: 'Express' });
});

module.exports = router;
// create - post
//update - put(content as a whole)/patch (properties of a content)
//delete - delete